1. 安装mysql，并导入sql.sql
2. 安装python，然后cd到backend目录，执行pip install -r requirements.txt，然后再cd到app目录，修改db.json中的数据库信息为自己的数据库信息，再执行：python app.py
3. 安装node，cd到frontend/ui目录，执行npm install，执行完毕后，再执行npm run dev，一般会自动跳出网页，如果不弹出，手动用浏览器访问http://localhost:9528，登录即可